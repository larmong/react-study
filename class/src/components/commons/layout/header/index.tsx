import styled from "@emotion/styled";

export default function LayoutHeader() {
  const Header = styled.header`
    height: 50px;
    background: #a14056;
  `;

  return <Header>여기는 Header 입니다</Header>;
}
