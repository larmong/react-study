function useQuery(aaa) {
    // aaa (ex, FECTH_BOARDS) 를 통해서 Backend에 API 요청함!

    return {
        data: { fetchBoards: { writer: "철수" } },
        loading: false,
        refetch: () => {
            console.log("리페치가 실행됩니다!")
        }
    }
}
undefined

const { data, refetch } = useQuery("FECTH_BOARDS")
undefined

data.fetchBoards
{writer: '철수'}

refetch()
리페치가 실행됩니다!
undefined
